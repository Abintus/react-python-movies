export default function ActorListItem(props) {
    return (
        <div>
            <div>
                <strong>{props.actor.name}</strong>
                {' '}
                <strong>{props.actor.surname}</strong>
                {' '}
                <input type={"checkbox"} onChange={props.onSelect}/>
            </div>
        </div>
    );
}